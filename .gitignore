# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Virtualenvs (tests-only dep: pytest)
.venv/
venv/
env/

# OS / editor cruft
.DS_Store
*.swp

# One-off internal QA/sweep drivers (not part of the skill surface); the
# evidence they produced is committed under docs/ (revalidate-progress.jsonl).
scripts/_revalidate_sweep.py
scripts/_retry_known_issue.py
